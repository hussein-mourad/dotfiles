git:
  overrideGpg: true
  diffRenderers:
    - colorArg: always
      # command: delta --dark --paging=never --no-gitconfig --line-numbers --navigate --side-by-side
      command: delta --dark --paging=never --no-gitconfig --line-numbers --navigate
  # pagers:
  #   - pager: delta --dark --paging=never --no-gitconfig --line-numbers --navigate
